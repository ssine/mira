# Home Server Compose requires this value. Keep it URL-safe because Compose
# embeds it in DATABASE_URL. Configure the administrator with the local
# `npm run admin -- set-password admin` command; never put that password here.
POSTGRES_PASSWORD=replace-with-a-long-random-password

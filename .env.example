# Home Server Compose requires both values. Replace them before starting it.
# Keep POSTGRES_PASSWORD URL-safe because Compose embeds it in DATABASE_URL.
THREAD_STORE_TOKEN=replace-with-a-long-random-token
POSTGRES_PASSWORD=replace-with-a-long-random-password
